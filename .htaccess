RewriteEngine On
RewriteBase /

# Исключаем реальные файлы
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Всё остальное — на index.php
RewriteRule ^(.*)$ index.php?q=$1 [L,QSA]